"""Wake-word models and sounds that ship inside the wheel.

`registry.py` records where every file here came from and under what terms;
`NOTICE.md` says the same thing in prose, and `sounds/LICENSE.md` is the licence
text CC BY 4.0 requires travel with the sounds. Nothing may be added to this
directory without a registry entry — `just check-assets` fails if it is.
"""
